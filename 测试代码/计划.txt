1.实现用pygame 加载图像
	pygame 不能直接加载numpy的pixel  需要转换一下
		可以用pil 但是考虑到以后需要用opencv  这里用opencv 转换  不行 还得用PIL
2、实现调窗
	实现调窗，并实现sigma窗 实现帧率调整
	界面显示窗宽，窗位
3、添加按钮实现自定义功能，与opencv sift 合作
	重构，先把event 继承 分开
	实现右键保存

	键盘事件
4开闭模糊等，完全完成，但代码难看，准备重构

5重构完成 ，以后需要添加图像处理函数的话，添加prododcmevent 最下方的函数  类似函数 返回图像，根据event变化图像，注意添加键盘按键事件

 pygame =2.1.2   2.1.3出错，需要用2.1.2 可能是3 没开放完成

发现bug 调整窗宽，窗位后 直接按s 用原始窗宽窗位的图计算sift  重构应该能好



6 添加翻页功能，计划添加一个数值，用来存储根目录下dcm文件夹下的文件列表，1.dcm   xx。dcm pageup 向上翻页，pagedown 向下翻页


7 更新架构，未来图像处理方法增加，每个方法都有参数，计划把鼠标的事件封装，mouse move 事件，返回两个数字，changex changy  上下左右独立 小范围改动数据，pgup pgdn 固定翻页用

目标图像处理函数传入参数1 image 返回参数 image 框架负责显示
	传入参数2，自己封装的event  包含 mous_change_x,mouse_change_y,key(up,down,left,right)

因为图像处理  多，事件多，需要一个图像处理类， 需要一个事件类，还需要一个管理类
管理类接受键盘的按键，更改当前事件，
管理类负责协调按键跟图像处理的对应关系

事件类负责pygame相关内容，pgup，pgdn ，        事件类必要时可以有个基类，基类实现界面更新等   其他事件传给管理类

管理类需要知道当前文件，
管理类根据事件分别调用不同的类处理，管理按键类，实现功能切换，维护按键及事件对应关系。    管理事件类，封装鼠标及上下左右，

重构完成，下一步翻页

   
 